---
scope: chess game analysis and improvement engine
purpose: build substrate for implementation agents
altitude: feature
updated: 2026-09-03T11:46
---

- (decision) AD-1: AWS RDS PostgreSQL for persistent game storage; binds all DB clients to psycopg2/asyncpg; prevents SQLite or in-memory stores that cannot survive ephemeral CI runners
- (decision) AD-2: Analysis runs as a separate GitHub Actions workflow, independent of video pipeline; prevents coupling analysis failures to video upload
- (decision) AD-3: Analysis pipeline fetches its own lost games directly from chess.com API; prevents dependency on video pipeline's won_games.json or quality filter
- (decision) AD-4: Window-based scope — each run analyzes last N lost games by time_control; N and time_control are env-var parameters (defaults: N=20, time_control=RAPID); prevents full history re-analysis on every run
- (decision) AD-5: Simple Claude integration — Python aggregates Stockfish output into structured data, single Claude API call generates the plan; prevents direct DB access or multi-turn tool use by Claude
- (decision) AD-6: Reports stored in S3 at path {bucket}/{username}/{time_control}/{date}.md; username prefix makes path multi-user ready without schema change; prevents committing reports to repo
- (decision) AD-7: Per-game Stockfish failure isolation — each game's analysis is wrapped independently; failed games are logged and skipped; prevents single game failure aborting the full run
- (decision) AD-8: Three-table schema — games(id, username, pgn, date, time_control, result, opponent_rating, fetched_at), evaluations(id, game_id, move_number, severity, best_move, eval_delta), reports(id, username, time_control, window_size, s3_path, generated_at); username on every user-owned table for multi-user readiness; prevents flat design that can't support per-move querying
- (constraint) Spec constraint inherited: analysis failures must not block video pipeline; enforced by separate workflow, not shared job
- (constraint) Spec constraint inherited: game data captured at fetch time, not reconstructed from video output; satisfied by AD-3 (own fetch)
- (constraint) Spec constraint inherited: Stockfish required for per-move evaluation; runs inside the analysis workflow on ubuntu-latest via apt-get install stockfish
- (assumption) Report format is Markdown (.md); Claude generates Markdown; S3 path uses .md extension
- (assumption) AWS credentials (RDS DB_URL, S3 bucket name + key) are stored as GitHub Actions secrets
- (assumption) Agentic Claude orchestration (from spec assumption) is retired; user confirmed simple single-call integration is sufficient
- (event) Spine distilled from memlog. Reviewer gate: lint + adversarial check pending.
- (decision) Reviewer gap: AD-8 clarified — evaluations never carry username; aggregate always joins evaluations→games on game_id
- (decision) Reviewer gap: eval_delta sign convention fixed — centipawns lost by player (eval_before − eval_after); always ≥ 0 for blunders/mistakes
- (decision) Reviewer gap: stage-level error handling added — fetch/RDS/S3 failures abort run with non-zero exit; no retry; per-game Stockfish failures continue run (AD-7)
- (decision) Reviewer gap: CHESS_USERNAME env var named explicitly (same as video pipeline); injected via workflow env: block
- (event) Reviewer gate passed after fixes. Spine finalized.
